# belt-quest — a factory-RPG for WildForge.
#
# Pure data + script mod (docs/port-plan.md decision 1). The factory is
# the crafting system, the settlement network is the quest infrastructure,
# and dungeons are destination content that hands materials back to the
# factory. Milestone B vertical slice: Haven, the first factory, modular
# gear, the Brood Gallery, and the first quest arc.

id = "belt_quest"
name = "belt-quest"
version = "0.1.0"
world_api = 2
depends = ["base"]
retrogen = "untouched_host_only"
